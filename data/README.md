This folder is intentionally kept lightweight for the public repo.

Local runtime files that should not be committed:
- `config.json` with personal API keys or machine-specific preferences
- `voices/` with reference voice samples
- `outputs/` with generated audio

Use `config.example.json` as the starting point for local setup.
